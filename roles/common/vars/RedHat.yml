---
# Only installed when common_manage_packages is enabled; the roles that
# need a specific tool (tar for etcd, procps-ng for keepalived,
# policycoreutils-python-utils for the SELinux port labels) install it
# themselves.
common_base_packages:
  - vim-enhanced
  - rsync
  - tar
  - unzip
  - jq
